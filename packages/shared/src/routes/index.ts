// ============================================================================
// @repo/shared — 路由模块统一导出
// ============================================================================
// 定义平台级路由表，供宿主应用的路由配置消费。
// RouteDefinition 包含路径、组件懒加载、元数据（权限、菜单关联等）。
// 宿主应用的 router/ 目录导入 routeDefinitions 并注册到 react-router。
// ============================================================================

export { routeDefinitions } from './definitions'
export type { RouteDefinition } from './definitions'
