{
  "//": "React 中后台前端平台 Monorepo 根包。所有 workspace 脚本通过 pnpm -F 或根脚本聚合执行。",
  "name": "frontend-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.1",
  "engines": {
    "node": "24.15.0"
  },
  "scripts": {
    "template:init": "node ./scripts/init-template.mjs",
    "template:check": "node ./scripts/check-template-residue.mjs . --no-fail",
    "dev:react": "pnpm -F @repo/react-app dev",
    "build:shared": "pnpm --filter @repo/shared-utils build && pnpm --filter @repo/shared-service build && pnpm --filter @repo/design-tokens build && pnpm --filter @repo/mock build && pnpm --filter @repo/shared-ui build",
    "build:react": "pnpm build:shared && pnpm -F @repo/react-app build",
    "build": "pnpm build:shared && pnpm --filter @repo/react-app build",
    "typecheck": "pnpm build:shared && pnpm --filter @repo/shared-utils typecheck && pnpm --filter @repo/shared-service typecheck && pnpm --filter @repo/design-tokens typecheck && pnpm --filter @repo/mock typecheck && pnpm --filter @repo/shared-ui typecheck && pnpm --filter @repo/react-app typecheck",
    "check:alias": "node ./scripts/check-alias-contract.mjs",
    "check:status": "node ./scripts/check-status.mjs",
    "check:phase2-consistency": "node ./scripts/check-phase2-consistency.mjs",
    "sync:mock-worker": "node ./scripts/sync-mock-worker.mjs",
    "verify:mock-worker": "node ./scripts/verify-mock-worker.mjs",
    "verify": "pnpm format:check && pnpm lint && pnpm stylelint && pnpm typecheck && pnpm check:alias && pnpm check:status && pnpm check:phase2-consistency && pnpm verify:mock-worker && pnpm test && pnpm test:scripts && pnpm build && pnpm template:check",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "stylelint": "stylelint \"apps/**/*.{scss,css}\" \"packages/**/*.{scss,css}\"",
    "stylelint:fix": "stylelint \"apps/**/*.{scss,css}\" \"packages/**/*.{scss,css}\" --fix",
    "test": "pnpm --filter @repo/shared-utils test && pnpm --filter @repo/shared-service test && pnpm --filter @repo/design-tokens test && pnpm --filter @repo/mock test && pnpm --filter @repo/shared-ui test && pnpm --filter @repo/react-app test",
    "test:watch": "node ./scripts/vitest.cjs",
    "test:scripts": "node --test \"scripts/__tests__/*.test.mjs\"",
    "test:coverage": "pnpm --filter @repo/shared-utils test:coverage && pnpm --filter @repo/shared-service test:coverage && pnpm --filter @repo/design-tokens test:coverage && pnpm --filter @repo/mock test:coverage && pnpm --filter @repo/shared-ui test:coverage && pnpm --filter @repo/react-app test:coverage",
    "prepare": "husky",
    "build:cross-platform-utils": "pnpm -F @repo/cross-platform-utils build",
    "dev:taro:weapp": "pnpm build:cross-platform-utils && pnpm -F @repo/taro-miniapp dev:weapp",
    "build:taro:weapp": "pnpm build:cross-platform-utils && pnpm -F @repo/taro-miniapp build:weapp",
    "dev:taro:h5": "pnpm build:cross-platform-utils && pnpm -F @repo/taro-miniapp dev:h5",
    "build:taro:h5": "pnpm build:cross-platform-utils && pnpm -F @repo/taro-miniapp build:h5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "msw"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "20.5.2",
    "@commitlint/config-conventional": "20.5.0",
    "@eslint/js": "9.39.4",
    "@tailwindcss/vite": "catalog:",
    "@vitest/coverage-v8": "4.1.5",
    "eslint": "9.39.4",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-react": "7.37.5",
    "eslint-plugin-react-hooks": "7.1.1",
    "husky": "9.1.7",
    "jsdom": "29.1.0",
    "lint-staged": "16.4.0",
    "postcss-html": "1.8.1",
    "prettier": "3.8.3",
    "sass": "1.99.0",
    "stylelint": "17.9.1",
    "stylelint-config-standard-scss": "17.0.0",
    "stylelint-order": "8.1.1",
    "stylelint-scss": "7.0.0",
    "tailwindcss": "catalog:",
    "typescript": "6.0.3",
    "typescript-eslint": "8.63.0",
    "vitest": "4.1.5"
  }
}
